function parseInput (input)
    if input == "cat" then
      systemOutput = "You typed the cat command."
    else
      systemOutput = "Syntax Error."
    end
--    table.insert(terminalOutput, systemOutput)
end
